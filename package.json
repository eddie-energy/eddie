{
  "name": "eddie",
  "private": true,
  "type": "module",
  "homepage": "https://eddie.energy",
  "scripts": {
    "build": "pnpm -r --filter=./region-connectors/* --filter=./core run build",
    "docs:build": "pnpm -C ./docs run build"
  },
  "engines": {
    "node": ">=18.14.0",
    "pnpm": ">=8.5.0"
  },
  "devDependencies": {
    "prettier": "^3.3.2"
  }
}
