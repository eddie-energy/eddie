{
  "cds-test": {
    "cds_server": "https://s-85248861.cds.utilityapi.com",
    "Security": {
      "Auth": {
        "cds": {
          "Type": "OAuth2",
          "Grant Type": "Client Credentials",
          "Client Credentials": "basic",
          "Client ID": "{{client-id}}",
          "Client Secret": "{{client-secret}}",
          "Token URL": "{{cds_server}}/oauth/token",
          "Scope": "client_admin"
        }
      }
    }
  }
}
